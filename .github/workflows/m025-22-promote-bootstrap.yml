name: m025-22 Proxy Bootstrap Promotion
on:
  push:
    branches:
      - master/perf39-real-wasm-proxy-m02522
    paths:
      - '.github/workflows/m025-22-promote-bootstrap.yml'
      - '.github/scripts/m02522-promote-bootstrap.py'
permissions:
  contents: write
jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply exact m025-22 bootstrap contract
        run: python .github/scripts/m02522-promote-bootstrap.py
      - name: Syntax check
        run: node --check features/neural-voice/fiezel-neural-voice-bootstrap.js
      - name: Commit bootstrap on repair branch
        shell: bash
        run: |
          git config user.name 'FIEZEL MASTER CI'
          git config user.email 'actions@users.noreply.github.com'
          git add features/neural-voice/fiezel-neural-voice-bootstrap.js
          if git diff --cached --quiet; then
            echo 'Bootstrap already promoted'
            exit 0
          fi
          git commit -m 'fix: enforce real Apple WASM proxy before session creation'
          git push origin HEAD:master/perf39-real-wasm-proxy-m02522
