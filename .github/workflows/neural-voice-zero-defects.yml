name: Neural Voice Zero Defects Repair

on:
  push:
    branches:
      - evidence/neural-voice-zero-defects

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run isolated repair harness
        shell: bash
        run: |
          cp tools/evidence/neural-voice-zero-defects-repair.sh /tmp/neural-voice-zero-defects-repair.sh
          cp tools/evidence/voice-audio-race-test.js /tmp/voice-audio-race-test.js
          bash /tmp/neural-voice-zero-defects-repair.sh /tmp/voice-audio-race-test.js
